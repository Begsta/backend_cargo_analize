# Правки после защиты / для отчёта ЛР-1

## Что изменили в коде

| Тема замечания | Как сделано |
|---|---|
| Поля: масса, объём | `cargo_mass`, `cargo_volume` (вместо вылета стрелы) |
| Имена в коде | префикс `cargo_*` |
| Кнопка | прямоугольная серая `#F2F2F2`, рамка `#E5E5E5` |
| Панель / карточки | белые `#FFFFFF` |
| Hover карточек | текст становится красным `#E30613` |
| Лента | параметры + иконки лайка и «Далее» **справа** |
| Репозиторий для лаб 5+ | фронт в `backend_cargo_analize/cargo_frontend/` (`templates/`, `static/`) |

Запуск только из бэка:

```bash
cd ~/WEB/backend_cargo_analize
python3 main.py
```

---

## Куда вставить в отчёт

### Блок «Тема / модель»
- Скрин `data/collections.py`: поля `cargo_mass`, `cargo_volume`.
- Текст: «Услуга (cargo) — вид груза. Тематические числовые поля: масса (т) и объём (м³).»

### Блок «Дизайн / CSS»
- Скрин шапки `static/css/style.css` (комментарий с цветами).
- Текст: «Скопировано с Wolffkran: красный `#E30613`, уголь `#1D1D1D`, белый `#FFFFFF`. Кнопка серая прямоугольная. Панель вкладок и карточки белые. Hover текста карточек — красный.»

### Скрины UI снова (после правок)
1. **Каталог** — серая «Отобрать», белые карточки, навести мышь → красный текст.
2. **Лента** — справа: масса/объём + сердце + иконка «Далее».
3. **Добавление** — поля масса и объём.

### Скрины 10–19 — переснять
Те же, что в методичке, но уже с новым UI:
- 10–12: Network GET (`/?cargo_mass=`, `/cargo/1`, next).
- 13–17: Response Minio + коллекция + 3 шаблона (пути: `cargo_frontend/templates/...`).
- 18–19: 3 URL и 3 контроллера в `api/handlers.py` + фильтр.

### Репозиторий
- В отчёте: «Для лаб 5+ фронт лежит в репозитории бэкенда: папка `cargo_frontend/` (`templates/`, `static/`).»
